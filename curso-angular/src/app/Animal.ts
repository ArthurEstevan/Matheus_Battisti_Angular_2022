// Usando uma propriedade do Typescript que é a interface
// conseguimos exportar um Objeto que deve seguir padrões
export interface Animal {
    id: number,
    name: string,
    type: string,
    age: number
}